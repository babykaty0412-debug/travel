# 🏮 平溪×深坑行程網站 — 作品集講稿

> 給履歷、面試、口頭 demo 用。**數字都是真的**，別加油添醋；面試官最愛的是**取捨判斷**與**除錯過程**。

---

## 🎯 一句話定位（Elevator pitch）

> 一個**資料驅動、離線可用**的旅遊行程網站：用**一份 `trip.json`** 驅動整個介面，並用 **vanilla TS 與 React 18 兩種實作**呈現同一套資料，全程 **CI 把關（型別／測試／無障礙／安全掃描）**。

---

## 📄 履歷條目（Résumé bullets）

**中文**
- 以 **TypeScript + Vite + Leaflet** 打造資料驅動的互動行程網站：單一 `trip.json` 驅動時間軸、互動地圖與 UI，改行程或換一趟旅程**只需改資料**。
- 用**零框架 vanilla TS** 與 **React 18 hooks** 兩種實作共用同一份資料／型別／CSS，展現框架取捨判斷；production JS 約 **50 KB gzip**。
- 建置完整 **GitHub Actions CI**：strict TypeScript、ESLint/Prettier、**JSON Schema 資料驗證**、Vitest 單元測試、**Playwright E2E ×10（含 axe 無障礙）**、Lighthouse CI、CodeQL + Dependabot + OpenSSF Scorecard。
- 安全與無障礙強化：build 注入 **CSP**、執行期資料守衛、XSS-safe 渲染、修正整套色盤達 **WCAG 2 AA**；以 Service Worker 提供 **PWA 離線**。

**English**
- Built a **data-driven, offline-capable** itinerary web app (**TypeScript, Vite, Leaflet**) where a single `trip.json` drives the timeline, interactive map, and UI — adding a stop or a whole new trip means editing data only.
- Delivered **two implementations** (zero-framework vanilla TS and **React 18** hooks) from the same data/types/CSS to demonstrate framework trade-offs; ~**50 KB gzip** JS.
- Stood up a full **GitHub Actions CI**: strict TS, ESLint/Prettier, **JSON-Schema data validation**, Vitest unit tests, **Playwright E2E (10, incl. axe a11y)**, Lighthouse CI, **CodeQL + Dependabot + OpenSSF Scorecard**.
- Hardened security & a11y: build-injected **CSP**, runtime data guard, XSS-safe rendering, and fixed **WCAG 2 AA** color-contrast across the palette; **PWA offline** via a service worker.

---

## 🗣️ 兩分鐘口頭講稿（STAR）

**情境**：一開始只是幫家人排「平溪→深坑」親子自駕行程。做成分享頁時，發現內嵌預覽（Artifact）有安全沙盒、**擋外部套件與地圖**。

**任務**：想要一個「真的互動地圖、能分享、好維護」的網站。

**行動 / 演進**：
1. 先做能動的**單檔 HTML**（700 行）→ 上 **GitHub Pages** 才能用真實 Leaflet 地圖。
2. 發現單檔難維護、資料寫死重複 → **重構成資料驅動**：抽出 `trip.json` 唯一資料來源，`render` 純函式同時產生時間軸與地圖。
3. 升級 **TypeScript + Vite**（型別安全、模組化）→ 補上 **CI／測試／CSP／無障礙**。
4. 最後用 **React 18** 重寫一版（同資料同 CSS），證明「有無框架都會、且知道何時該用」。

**結果**：**CI 全綠**（8 道關卡）、7 單元 + 10 E2E、WCAG 2 AA、PWA 離線、雙實作；改行程只動一個 JSON、換旅程可重用整套介面。

---

## 🔧 三個「真實除錯故事」（面試殺手鐧）

1. **CI 卡在 60 秒逾時**：Playwright 的 webServer 一直等不到 `vite preview`。原因是 **preview 綁在 IPv6 `localhost`，而 Playwright 輪詢 IPv4 `127.0.0.1`** → 兩邊對不上。修法：`--host 127.0.0.1 --strictPort`。**教訓**：本機過、CI 掛，先想環境差異（host/port、時區、字型）。

2. **CSP 卻讓頁面白屏（差點）**：加了 `script-src 'self'` 後，Vite 預設的 **modulePreload polyfill 會注入一段 inline script** 違反 CSP。修法：`build.modulePreload.polyfill = false`（現代瀏覽器原生支援）→ 建置產物只剩一支外部 module script，CSP 過關。

3. **無障礙的「假通過」**：axe 一開始說 vanilla 版沒問題，但那是因為**捲動淡入動畫讓文字還半透明、被 axe 當成不可見而跳過**。改用 `reducedMotion` 掃描後，抓出**整套品牌色對比不足**（橘 3.4:1）→ 把 accent/green/faint 加深到全數 **≥4.5:1**。**教訓**：綠燈不等於沒問題，要懂工具的量測前提。

---

## ❓ 可能被追問（先準備好）

- **為什麼不用框架 / 何時該用？** 這個規模 vanilla 就夠，導入 React 反增複雜度；但為展示能力做了平行 React 版。框架的價值在**狀態多、團隊協作、生態**時才顯著。
- **資料驅動的代價？** 動態渲染有極小的執行期成本、SEO 需注意（此站以 CSR 為主）；換來的是**單一真相來源**與可重用。
- **安全標頭在 GitHub Pages？** Pages 不支援自訂 header，所以 CSP 用 `<meta>` 注入；完整標頭放 `_headers`，換 Netlify/Cloudflare 生效 —— 知道**平台限制**與**替代方案**。
- **效能怎麼顧？** bundle 小（~50KB gzip）、Leaflet 已 bundle 無 CDN 往返、圖磚與路線失敗都有 fallback、Lighthouse CI 持續量測。
- **測試策略？** 純邏輯用 Vitest（快、無 DOM）、行為用 Playwright E2E、無障礙用 axe、資料用 JSON Schema + 執行期守衛 —— **分層驗證**。

---

## 📊 技術速記表

| 面向 | 內容 |
|---|---|
| 前端 | TypeScript（strict）、Vite 5、Leaflet、React 18、原生 Web API（View Transitions／IntersectionObserver／Service Worker） |
| 架構 | 資料驅動（`trip.json` 單一來源）、模組化、防護性初始化（`safe()`） |
| 測試 | Vitest ×7、Playwright ×10（含 axe） |
| CI/CD | GitHub Actions：型別／lint／格式／schema／audit／build／E2E／Lighthouse；main 自動部署 Pages |
| 安全 | CSP、輸入 escape、`assertTrip`、CodeQL、Dependabot、Scorecard、`_headers` |
| 無障礙 | WCAG 2 AA（axe 驗證、色彩對比達標、`prefers-reduced-motion`、`aria-current`） |
| PWA | Service Worker 離線、manifest、加入主畫面 |
| 產物 | vanilla JS ~4.6KB + React ~49KB（gzip）；CSS ~10KB |
| 連結 | Live（vanilla `/travel/`、React `/travel/react/`）、原始碼、CI 徽章 |

---

## 🔗 放進履歷的一行連結格式

```
平溪×深坑 行程網站 — 資料驅動 / TS+Vite / React / CI / a11y / PWA
GitHub: github.com/babykaty0412-debug/travel　·　Demo: babykaty0412-debug.github.io/travel/
```
